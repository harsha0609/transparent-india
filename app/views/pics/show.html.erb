<h1>@pic.title</h1>
<h2>@pic.domain </h2>
<p>@pic.content</p>

<!-- %h1.title= @pic.title
%h2.domain=@pic.domain
%p= simple_format(@pic.content) -->


<div class= "buttons">
<%= link_to "Edit", edit_pic_path(@pic), class: "button" %>
<%= link_to "Trash It", pic_path(@pic), method: :delete, data: {confirm: "Are you sure?"},class: "button"%>
</div>
